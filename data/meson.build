

i18n.merge_file(
    input: 'sessionsaver-plugin.desktop.in',
    output: 'sessionsaver.plugin',
    po_dir: join_paths(meson.source_root(), 'po'),
    type: 'desktop',
    install: true,
    install_dir: pluginslibdir,
)